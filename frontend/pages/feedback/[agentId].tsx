import { useRouter } from 'next/router';
import { useState } from 'react';
import axios from 'axios';
export default function FeedbackPage(){const router=useRouter();const { agentId }=router.query;const { callId, logId }=router.query;const [rating,setRating]=useState(0);const [comment,setComment]=useState('');const submit=async()=>{await axios.post(`${process.env.NEXT_PUBLIC_API_URL}/api/feedback/submit`,{agentId,callId,logId,rating,comment});router.push('/thank-you');};return(<div className="mx-auto mt-12 max-w-md text-center"><h2 className="mb-4 text-xl font-semibold">Rate your call</h2><div className="flex justify-center space-x-2 mb-4">{[1,2,3,4,5].map(s=>(<button key={s} onClick={()=>setRating(s)} className={s<=rating?'text-yellow-400':'text-gray-400'}>★</button>))}</div><textarea className="w-full border rounded p-2 mb-4" placeholder="Optional comment" value={comment} onChange={e=>setComment(e.target.value)}/><button onClick={submit} className="px-4 py-2 rounded bg-blue-600 text-white">Submit</button></div>);} 
